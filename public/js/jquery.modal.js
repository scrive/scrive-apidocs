(function($){
	
	$.fn.modal = function(o) {
		var defaults = {
			height: '390px',
			width: '480px',
		};
		
		if(o) $.extend(defaults, o);
		
		return this.each( function() {
			
		});
	};
})(jQuery);