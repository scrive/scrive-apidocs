---
get:

  summary: Get data retention policies

  tags:
    - User

  description: |
    Returns an object with user and company data detention policies

  responses:
    200:
      description: Returns a data retention policy
      schema:
        $ref: ../../scrive_api.yaml#/definitions/DataRetentionPolicies
