---
get:

  summary: Get tags

  tags:
    - User

  description: |
    Returns tags attached to the user

  responses:
    200:
      description: 'Returns tags for the given user'
      schema:
        $ref: ../../scrive_api.yaml#/definitions/Tags
