---
title: SignatoryFieldPlacementTip
type: string
description: |
  From which side the arrow on the signing page should point
  to the field.

  Can be `null`, in which case the direction is decided by the field type.
enum:
  - left
  - right
