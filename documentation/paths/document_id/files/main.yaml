---
get:
  summary: Get the main file

  tags:
    - Get

  description: |
    **Get the main PDF file for a document.**

    The `filename` parameter in the URL can be set to any valid file name.
    This allows you to download the file with user-specified file name.

    *OAuth Privileges required: `DOC_CHECK`*

  parameters:
    - $ref: ../../../parameters.yaml#/DocumentID
    - $ref: ../../../parameters.yaml#/SignatoryParamForFile

  responses:
    200:
      description: The PDF file.
    # FIXME
    # $document_id$ is closed but sealed PDF is not yet available
    # 503 Service Unavailable
    # document_state_error
    # The sealed PDF for the document is not ready yet, please wait and try again.
