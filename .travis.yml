sudo: false

language: ruby

rvm:
  - 2.1.0

cache: bundler
script: bundle exec middleman build

deploy:
  skip_cleanup: true
  provider: script
  script: rvm $TRAVIS_RUBY_VERSION do deploy.sh
  on:
    branch: master
