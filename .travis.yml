sudo: false

language: ruby

rvm:
  - 2.1.0

cache: bundler
script: bundle exec middleman build --clean

deploy:
  skip_cleanup: true
  provider: script
  script: ./deploy.sh --no-build
  on:
    branch: master
