/** @jsx React.DOM */

define(['React', 'Backbone'], function(React, Backbone) {
  var expose = {};
  
  expose.LandingPage = React.createClass({
    render: function() {
      return (
	  <div>Wtf</div>
      );
    }
  });
  
  return expose;
});
