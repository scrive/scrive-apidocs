define(["legacy_code", "Underscore", "Backbone", "React"], function (legacy_code, _, Backbone, React) {
  return React.createClass({
    render: function () {
      return (
        <span>
          <h1>Nowergian BankID Iframe</h1>
        </span>
      );
    }
  });
});
