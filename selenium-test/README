* setup
to install all python dependencies just run:
pip install -r requirements.txt
using a virtualenv is highly encouraged!
* running
cd into any subfolder (except for utils) and start run.py script,
it will tell you about required/optional arguments and how to
setup configuration
